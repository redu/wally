check process wally with pidfile /data/wally/current/tmp/wally.pid
  start = "/etc/monit.d/wally_app_wrapper.sh start" as uid deploy and gid deploy
  stop = "/etc/monit.d/wally_app_wrapper.sh stop" as uid deploy and gid deploy
