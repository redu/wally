check process wally with pidfile /data/wally/current/tmp/wally.pid
  start = "/etc/monit.d/permit_app_wrapper.sh start" as uid deploy and gid deploy
  stop = "/etc/monit.d/permit_app_wrapper.sh stop" as uid deploy and gid deploy
